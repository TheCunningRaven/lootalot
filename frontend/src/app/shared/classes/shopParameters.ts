export class ShopParameters {
    // We use a class because classes allow us to intialise values when creating an instance
    sort = 'name';
    brandId = 0;
    typeId = 0;
    pageNumber = 1;
    pageSize = 6;
}